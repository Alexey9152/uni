# Веб-приложение "Библиотека" - Spring Boot

Полнофункциональное веб-приложение для управления библиотекой с двумя типами пользователей: Библиотекарь и Читатель.

## 🎯 Возможности

### Для Библиотекаря:
- ✅ Просмотр всех книг в таблице
- ✅ Добавление новых книг
- ✅ Переоценка (изменение цены) книг
- ✅ Выдача книг читателям
- ✅ Просмотр всех читателей и их аккаунтов
- ✅ Все данные автоматически сохраняются в XML

### Для Читателя:
- ✅ Просмотр каталога книг
- ✅ Поиск по автору
- ✅ Поиск по году издания
- ✅ Поиск по категории
- ✅ Просмотр своего аккаунта
- ✅ Просмотр выданных ему книг

### Общие функции:
- ✅ Аутентификация и авторизация
- ✅ Регистрация новых пользователей
- ✅ Сохранение в XML (DOM парсер)
- ✅ Валидация по XSD схеме
- ✅ Шифрование паролей (SHA-256 + соль)
- ✅ Разделение прав доступа

## 🛠 Технологический стек

- **Backend**: Spring Boot 3.1.5
- **Security**: Spring Security 6
- **Frontend**: Thymeleaf (HTML + CSS + JavaScript)
- **XML**: DOM Parser, XSD Validation
- **Java Version**: 17+
- **Build**: Maven 3.6+

## 📋 Требования

- Java 17 или выше
- Maven 3.6+
- Браузер с поддержкой JavaScript

## 🚀 Установка и запуск

### 1. Клонирование/Распаковка проекта

```bash
cd library-web-app
```

### 2. Сборка проекта

```bash
mvn clean install
```

### 3. Запуск приложения

```bash
mvn spring-boot:run
```

Или:

```bash
java -jar target/library-web-app-1.0.0.jar
```

### 4. Открыть в браузере

```
http://localhost:8080/
```

Приложение автоматически перенаправит на страницу логина.

## 👤 Тестовые учетные записи

После первого запуска создайте аккаунты через форму регистрации:

**Вариант 1: Регистрация вручную**
- Перейдите на страницу регистрации
- Введите данные
- Выберите роль (Библиотекарь/Читатель)

**Вариант 2: Инициализация тестовых данных**

Отредактируйте `XmlService.java` для добавления тестовых пользователей при запуске приложения.

## 📂 Структура файлов

```
library-web-app/
├── src/main/java/com/library/
│   ├── LibraryApplication.java           # Main класс
│   ├── config/
│   │   └── SecurityConfig.java           # Конфиг безопасности
│   ├── controller/
│   │   ├── AuthController.java           # Контроллер регистрации
│   │   ├── ViewController.java           # Контроллер представлений
│   │   ├── LibrarianController.java      # API для библиотекаря
│   │   └── ReaderController.java         # API для читателя
│   ├── model/
│   │   ├── Book.java                     # Модель книги
│   │   ├── User.java                     # Модель пользователя
│   │   └── UserRole.java                 # Перечисление ролей
│   ├── service/
│   │   ├── BookService.java              # Сервис книг
│   │   ├── UserService.java              # Сервис пользователей
│   │   ├── XmlService.java               # XML операции
│   │   └── EncryptionService.java        # Шифрование паролей
│   └── dto/                              # Data Transfer Objects
├── src/main/resources/
│   ├── application.properties            # Конфиг приложения
│   ├── templates/
│   │   ├── login.html                    # Страница входа
│   │   ├── register.html                 # Страница регистрации
│   │   ├── librarian-dashboard.html      # Панель библиотекаря
│   │   └── reader-dashboard.html         # Панель читателя
│   └── data/
│       ├── library.xml                   # Данные книг
│       ├── library.xsd                   # Схема книг
│       ├── users.xml                     # Данные пользователей
│       └── users.xsd                     # Схема пользователей
├── pom.xml                               # Maven конфиг
└── README.md                             # Этот файл
```

## 🔐 Безопасность

### Шифрование паролей
- Пароли хэшируются с использованием SHA-256
- Каждый пароль имеет уникальную соль (16 байт)
- При проверке пароль хэшируется и сравнивается с сохраненным хэшем

### Контроль доступа
- Spring Security управляет авторизацией
- Разные endpoint'ы доступны для разных ролей
- Аутентификация требуется для всех операций кроме регистрации/логина

## 💾 Работа с XML

### DOM Parser
Приложение использует DOM парсер для:
- **Загрузки** данных из XML файлов
- **Сохранения** изменений в XML
- **Обхода** структуры документа с использованием NodeList

### XSD Валидация
XML документы валидируются по XSD схемам перед обработкой:
- `library.xsd` - для структуры книг
- `users.xsd` - для структуры пользователей

### Пример структуры library.xml:
```xml
<?xml version="1.0" encoding="UTF-8"?>
<library>
    <book id="1" total="5" available="3">
        <title>Война и мир</title>
        <author>Лев Толстой</author>
        <year>1869</year>
        <category>Роман</category>
        <price>450.00</price>
    </book>
</library>
```

### Пример структуры users.xml:
```xml
<?xml version="1.0" encoding="UTF-8"?>
<users>
    <user>
        <username>librarian1</username>
        <password>хэш_пароля</password>
        <fullName>Иван Иванов</fullName>
        <role>LIBRARIAN</role>
        <borrowedBooks>
        </borrowedBooks>
    </user>
</users>
```

## 🔄 API Endpoints

### Для Библиотекаря

| Метод | Endpoint | Описание |
|-------|----------|---------|
| GET | `/api/librarian/books` | Получить все книги |
| POST | `/api/librarian/books` | Добавить новую книгу |
| PUT | `/api/librarian/books/{id}/price` | Изменить цену |
| POST | `/api/librarian/books/{bookId}/issue` | Выдать книгу |
| GET | `/api/librarian/readers` | Получить всех читателей |

### Для Читателя

| Метод | Endpoint | Описание |
|-------|----------|---------|
| GET | `/api/reader/books` | Получить все книги |
| GET | `/api/reader/books/search/author?author=` | Поиск по автору |
| GET | `/api/reader/books/search/year?year=` | Поиск по году |
| GET | `/api/reader/books/search/category?category=` | Поиск по категории |
| GET | `/api/reader/account` | Получить свой аккаунт |
| GET | `/api/reader/borrowed-books` | Получить выданные книги |

## 🐛 Известные проблемы и решения

### Проблема: XML файлы не загружаются
**Решение**: Убедитесь, что директория `src/main/resources/data/` существует и содержит файлы:
```bash
mkdir -p src/main/resources/data/
```

### Проблема: Ошибка валидации XML
**Решение**: Проверьте, что XSD файлы находятся в правильной директории и содержат корректные схемы.

### Проблема: Пароль не совпадает при логине
**Решение**: Используйте ту же версию EncryptionService для регистрации и проверки.

## 📝 Примеры использования

### Регистрация нового библиотекаря:
1. Перейти на `/register`
2. Заполнить форму:
   - Username: `librarian1`
   - Password: `password123`
   - Full Name: `Иван Иванов`
   - Role: `Библиотекарь`
3. Нажать "Зарегистрироваться"
4. Перейти на `/login` и войти

### Добавление книги:
1. Войти как библиотекарь
2. Перейти на вкладку "Добавить книгу"
3. Заполнить все поля
4. Нажать "Добавить книгу"
5. Данные автоматически сохранятся в `library.xml`

### Выдача книги читателю:
1. На вкладке "Выдать книгу" выбрать книгу
2. Ввести username читателя
3. Нажать "Выдать книгу"
4. Количество книг уменьшится, аккаунт читателя обновится

## 🎓 Образовательные цели

Этот проект демонстрирует:
- ✅ Spring Boot приложение с Security
- ✅ Работа с XML (DOM Parser, XPath, XSD)
- ✅ Многоуровневая архитектура (Controller → Service → DAO)
- ✅ Аутентификация и авторизация
- ✅ RESTful API
- ✅ Разделение прав доступа (RBAC)
- ✅ Шифрование и безопасность паролей

## 📞 Поддержка

Если возникнут вопросы, проверьте:
1. Версию Java (должна быть 17+)
2. Наличие Maven
3. Права доступа к файлам `data/`
4. Консоль приложения для сообщений об ошибках

## 📄 Лицензия

Учебный проект. Свободен для использования в образовательных целях.

---

**Версия**: 1.0.0  
**Автор**: Alexey (CS Student)  
**Дата**: Декабрь 2025
